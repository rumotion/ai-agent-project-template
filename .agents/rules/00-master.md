# Antigravity Master Rule

Read `AGENTS.md` first; it is canonical. Then read `memory-bank/startup.md`.

Lazy-load Memory Bank files, workflows, skills, and docs only when relevant. Treat `@AGENTS.md` as the primary source if file mentions are supported.