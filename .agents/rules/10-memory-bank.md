# Antigravity Memory Bank Rule

Use `memory-bank/` as the durable project knowledge base.

Read `memory-bank/startup.md` before work. Use `memory-bank/00-index.md` only to choose additional relevant files. Update only relevant Memory Bank files after meaningful changes.