# Antigravity Safety Rule

Never expose or modify secrets without explicit approval.

Ask before destructive operations, production-impacting commands, dependency installs, migrations, or broad refactors.

Avoid generated files, dependencies, caches, build outputs, binaries, and huge logs unless required.