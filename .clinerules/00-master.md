# Cline Master Rule

Read `AGENTS.md` first; it is canonical. Then read `memory-bank/startup.md`.

Lazy-load Memory Bank files, workflows, skills, and docs only when relevant. Do not duplicate or override `AGENTS.md` here.