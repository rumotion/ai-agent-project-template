# Cline Memory Bank Rule

Use the Memory Bank in `memory-bank/` as durable project context.

Start each task with `memory-bank/startup.md`. Use `memory-bank/00-index.md` only to choose additional relevant files.

After meaningful work, update only the Memory Bank files that changed. Keep updates concise and operational.