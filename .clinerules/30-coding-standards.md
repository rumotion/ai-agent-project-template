# Cline Coding Standards Rule

Follow `AGENTS.md` coding and architecture standards.

Prefer small, maintainable, testable changes that match the surrounding code style.

Do not introduce new dependencies or architectural patterns unless the task justifies them.