# API

Document public APIs, internal service contracts, schemas, endpoints, events, or CLI interfaces here after this template is initialized for a real project.

Keep concise API-contract context in `memory-bank/systemPatterns.md` when it affects agent implementation decisions.